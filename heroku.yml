setup:
  addons:
    - plan: heroku-postgresql
    - plan: redistogo
build:
  docker:
    web: Dockerfile

run:
  web: bundle exec rails s -p $PORT
  worker:
    command:
      - bundle exec sidekiq -c 2
    image: web