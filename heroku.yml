setup:
  addons:
    - plan: heroku-postgresql
    - plan: redistogo
build:
  docker:
    web: Dockerfile

run:
  web: bundle exec rails s -p $PORT
  worker:
    command:
      - bundle install
      - bundle exec sidekiq
    image: web