setup:
  addons:
    - plan: heroku-postgresql
    - plan: redistogo
build:
  docker:
    web: Dockerfile

run:
  web: bundle exec rails s -p $PORT
  command:
    - bundle install
  worker:
    command:
      - bundle install
      - bundle exec sidekiq
    image: web